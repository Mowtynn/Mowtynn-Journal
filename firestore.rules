rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Requires authenticated user for all database read and write operations
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}




